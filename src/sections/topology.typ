#import "../callouts.typ": *

= Network Topology <section_topology>
#plan[
	+ gpu resources Tflops needed . and do some cost analysis with google colab
	+ currently most datasets prevent deployment.
	+ but we can predict and stay ahead by calculating the cost of total fine tuning
]

#lorem(100)
